# 📊 Документация по импорту и экспорту данных

## Содержание
1. [Общая информация](#общая-информация)
2. [Импорт организационной структуры](#импорт-организационной-структуры)
3. [Импорт сотрудников](#импорт-сотрудников)
4. [Импорт оборудования](#импорт-оборудования)
5. [Экспорт данных](#экспорт-данных)
6. [Обработка ошибок](#обработка-ошибок)
7. [Примеры файлов](#примеры-файлов)
8. [FAQ](#faq)

---

## Общая информация

### Назначение
Система импорта/экспорта позволяет:
- **Массово загружать** организационную структуру (организации, подразделения, отделы, должности)
- **Массово загружать** данные сотрудников с автоматическим созданием связанных объектов
- **Массово загружать** оборудование с автоматической генерацией инвентарных номеров
- **Выгружать** существующие данные в Excel для редактирования и повторного импорта

### Поддерживаемые форматы
- ✅ **XLSX** (Excel 2007 и выше) - рекомендуется
- ✅ **XLS** (Excel 97-2003)
- ❌ CSV - не поддерживается

### Доступ к функционалу
1. Войдите в систему под учетной записью администратора
2. Перейдите в нужный раздел Django Admin:
   - **Профессии и должности** → кнопки "📥 Импорт" и "📤 Экспорт"
   - **Сотрудники** → кнопки "📥 Импорт" и "📤 Экспорт"
   - **Оборудование** → кнопки "📥 Импорт" и "📤 Экспорт"
3. Выберите нужное действие (импорт или экспорт)

---

## Импорт организационной структуры

### Описание
Позволяет импортировать полную иерархию:
```
Organization (Организация)
  ↓
 StructuralSubdivision (Структурное подразделение)
  ↓
Department (Отдел)
  ↓
Position (Профессия/должность)
```

### Структура Excel-файла

#### Всего 9 столбцов:

| # | Название столбца | Описание | Обязательное | Пример |
|---|-----------------|----------|--------------|--------|
| 1 | `org_short_name_ru` | Краткое наименование организации (рус) | ✅ Да | РиК |
| 2 | `subdivision_name` | Наименование подразделения | ❌ Нет | Цех №1 |
| 3 | `department_name` | Наименование отдела | ❌ Нет | Участок сборки |
| 4 | `position_name` | Название должности | ✅ Да | Слесарь 4 разряда |
| 5 | `safety_instructions_numbers` | Номера инструкций по ОТ | ❌ Нет | И-001, И-025 |
| 6 | `internship_period_days` | Срок стажировки (дни) | ❌ Нет | 5 |
| 7 | `is_responsible_for_safety` | Ответственный за ОТ | ❌ Нет | да/нет |
| 8 | `can_be_internship_leader` | Может быть руководителем стажировки | ❌ Нет | да/нет |
| 9 | `can_sign_orders` | Может подписывать распоряжения | ❌ Нет | да/нет |

**Примечания:**
- Если `subdivision_name` не указано, должность привязывается только к организации
- ⚠️ Нельзя указать отдел (`department_name`) без структурного подразделения (`subdivision_name`)
- Полное наименование организации, группа электробезопасности и другие поля заполняются вручную после импорта

**Примечания к булевым полям:**
Поддерживаются следующие значения:
- **Истина:** да, yes, 1, true, т, y
- **Ложь:** нет, no, 0, false, н, n, (пустая ячейка)

### Логика работы импорта

1. **Каскадное создание объектов:**
   - Перед импортом каждой строки система автоматически создает/находит организацию по `org_short_name_ru`
   - Если организация не найдена → создается новая с минимальными данными:
     - `full_name_ru` = `short_name_ru`
     - `short_name_by` = `short_name_ru`
     - `full_name_by` = `short_name_ru`
     - `location` = "г. Минск"
   - Аналогично для подразделений и отделов

2. **Обработка существующих должностей:**
   - Поиск по комбинации: `(position_name, organization, subdivision, department)`
   - Если найдена → **обновляются все поля**
   - Если не найдена → **создается новая**

3. **Валидация:**
   - Проверка обязательных полей (`org_short_name_ru`, `position_name`)
   - Проверка логики: нельзя указать отдел без подразделения
   - Автоматическая установка значений по умолчанию:
     - `internship_period_days` → 0 (если пусто)
     - `is_responsible_for_safety` → False (если пусто)
     - `can_be_internship_leader` → False (если пусто)
     - `can_sign_orders` → False (если пусто)

4. **Важно:**
   - Организации создаются автоматически, даже если их нет в базе
   - В режиме предпросмотра все изменения откатываются (транзакция)
   - После подтверждения импорта все данные сохраняются в базу

### Пример Excel-файла для импорта структуры

| org_short_name_ru | subdivision_name | department_name | position_name | safety_instructions_numbers | internship_period_days | is_responsible_for_safety | can_be_internship_leader | can_sign_orders |
|-------------------|------------------|-----------------|---------------|----------------------------|------------------------|---------------------------|--------------------------|-----------------|
| РиК | Цех №1 | Участок сборки | Слесарь | И-001 | 5 | нет | да | нет |
| РиК | Цех №1 | Участок сборки | Токарь 4 разряда | И-002 | 10 | нет | да | нет |
| РиК | Офис | | Главный бухгалтер | | 0 | нет | нет | да |
| РиК | | | Директор | | 0 | да | нет | да |

---

## Импорт сотрудников

### Описание
Позволяет массово загружать данные сотрудников с автоматическим созданием:
- Организаций
- Структурных подразделений
- Отделов
- Должностей

### Структура Excel-файла

#### Всего 6 столбцов:

| # | Название столбца | Описание | Обязательное | Автозаполнение | Пример |
|---|-----------------|----------|--------------|----------------|--------|
| 1 | `hire_date` | Дата приема | ✅ Да | - | 15.01.2020 |
| 2 | `org_short_name_ru` | Краткое наименование организации | ✅ Да | - | РиК |
| 3 | `subdivision_name` | Структурное подразделение | ❌ Нет | - | Цех №1 |
| 4 | `department_name` | Отдел | ❌ Нет | - | Участок сборки |
| 5 | `position_name` | Название должности | ✅ Да | - | Слесарь |
| 6 | `full_name_nominative` | ФИО (именительный падеж) | ✅ Да | - | Иванов Иван Иванович |

### Автоматическое заполнение полей

При импорте система **автоматически** заполняет следующие поля:

| Поле | Значение | Описание |
|------|----------|----------|
| `start_date` | = `hire_date` | Дата начала работы = дата приема |
| `contract_type` | `'standard'` | Тип договора: трудовой договор |
| `status` | `'active'` | Статус: оформлен (работающий) |
| `full_name_dative` | **автогенерация** | ФИО в дательном падеже через pymorphy2 |

**Примечание:** Дата рождения, место проживания, размеры одежды и другие поля заполняются вручную после импорта.

### Автоматическая генерация ФИО в дательном падеже

Система использует библиотеку **pymorphy2** для автоматического склонения ФИО.

**Примеры автогенерации:**
- Иванов Иван Иванович → Иванову Ивану Ивановичу
- Петрова Мария Сергеевна → Петровой Марии Сергеевне
- Сидоров Алексей → Сидорову Алексею

### Логика работы импорта сотрудников

1. **Поиск или создание организационной структуры:**
   - Ищется организация по `org_short_name_ru`
   - Если не найдена → создается минимальная организация
   - Аналогично для подразделений, отделов, должностей

2. **Обработка сотрудников:**
   - Поиск по `full_name_nominative`
   - Если найден → **обновляются все данные**
   - Если не найден → **создается новый сотрудник**

3. **Автозаполнение:**
   - `start_date` копируется из `hire_date`
   - `contract_type` устанавливается в `'standard'`
   - `status` устанавливается в `'active'`
   - `full_name_dative` генерируется автоматически

### Пример Excel-файла для импорта сотрудников

| hire_date | org_short_name_ru | subdivision_name | department_name | position_name | full_name_nominative |
|-----------|-------------------|------------------|-----------------|---------------|---------------------|
| 15.01.2020 | РиК | Цех №1 | Участок сборки | Слесарь | Иванов Иван Иванович |
| 20.02.2020 | РиК | Цех №1 | Участок сборки | Токарь | Петров Петр Петрович |
| 01.03.2021 | РиК | Офис | | Бухгалтер | Сидорова Мария Ивановна |
| 10.04.2021 | РиК | | | Директор | Козлов Сергей Александрович |

---

## Импорт оборудования

### Описание
Позволяет массово загружать оборудование с автоматическим созданием:
- Организаций
- Структурных подразделений
- Отделов
- Автоматической генерацией инвентарных номеров

### Структура Excel-файла

#### Всего 7 столбцов:

| # | Название столбца | Описание | Обязательное | Автозаполнение | Пример |
|---|-----------------|----------|--------------|----------------|--------|
| 1 | `org_short_name_ru` | Краткое наименование организации | ✅ Да | - | РиК |
| 2 | `subdivision_name` | Структурное подразделение | ❌ Нет | - | Цех №1 |
| 3 | `department_name` | Отдел | ❌ Нет | - | Участок сборки |
| 4 | `equipment_name` | Наименование оборудования | ✅ Да | - | Токарный станок 16К20 |
| 5 | `inventory_number` | Инвентарный номер (8 цифр) | ❌ Нет | автоген. | 00000001 |
| 6 | `maintenance_period_months` | Периодичность ТО (месяцев) | ❌ Нет | - | 12 |
| 7 | `last_maintenance_date` | Дата последнего ТО (ДД.ММ.ГГГГ) | ❌ Нет | - | 15.01.2025 |

### Автоматическое заполнение полей

При импорте система **автоматически** заполняет следующие поля:

| Поле | Значение | Описание |
|------|----------|----------|
| `inventory_number` | **автогенерация** | 8-значный номер (00000001, 00000002, ...) если не указан |
| `next_maintenance_date` | **автоматический расчет** | Рассчитывается из last_maintenance_date + maintenance_period_months |
| `maintenance_status` | `'operational'` | Статус: исправно |

**Примечание:** Если `maintenance_period_months` не указан, поле остается пустым (NULL).

### Автоматическая генерация инвентарных номеров

Если поле `inventory_number` пустое, система автоматически генерирует уникальный 8-значный номер:
- Формат: `00000001`, `00000002`, `00000003` и т.д.
- Система находит последний использованный номер и увеличивает на 1
- Только цифры, без букв и префиксов

### Логика работы импорта оборудования

1. **Поиск или создание организационной структуры:**
   - Ищется организация по `org_short_name_ru`
   - Если не найдена → создается минимальная организация
   - Аналогично для подразделений, отделов

2. **Обработка оборудования:**
   - Поиск по `inventory_number`
   - Если найдено → **обновляются все данные**
   - Если не найдено → **создается новое оборудование**

3. **Автозаполнение:**
   - `inventory_number` генерируется автоматически (если пустой)
   - `next_maintenance_date` рассчитывается автоматически (если указаны обе даты)
   - `maintenance_status` устанавливается в `'operational'`

### Пример Excel-файла для импорта оборудования

| org_short_name_ru | subdivision_name | department_name | equipment_name | inventory_number | maintenance_period_months | last_maintenance_date |
|-------------------|------------------|-----------------|----------------|------------------|---------------------------|-----------------------|
| РиК | Цех №1 | Участок сборки | Токарный станок 16К20 | 00000001 | 12 | 15.01.2025 |
| РиК | Цех №1 | Участок сборки | Фрезерный станок | | 6 | 01.02.2025 |
| РиК | Цех №2 | | Кран-балка 5т | 00000050 | 12 | 10.12.2024 |

---

## Экспорт данных

### Экспорт организационной структуры

**Как экспортировать:**
1. Перейдите: **Django Admin → Профессии и должности**
2. Нажмите кнопку **"📤 Экспорт"** в правом верхнем углу
3. Файл `organization_structure.xlsx` будет загружен

**Что экспортируется:**
- Все должности (Position) с полной иерархией
- Все 9 столбцов в том же формате, что и для импорта
- Данные готовы для редактирования и повторного импорта

**Использование:**
- Экспортируйте текущие данные
- Отредактируйте в Excel
- Импортируйте обратно для массового обновления

### Экспорт сотрудников

**Как экспортировать:**
1. Перейдите: **Django Admin → Сотрудники**
2. Нажмите кнопку **"📤 Экспорт"** в правом верхнем углу
3. Файл `employees.xlsx` будет загружен

**Что экспортируется:**
- Все сотрудники с полными данными
- Все 6 столбцов в том же формате, что и для импорта
- Включая автосгенерированные значения

### Экспорт оборудования

**Как экспортировать:**
1. Перейдите: **Django Admin → Оборудование**
2. Нажмите кнопку **"📤 Экспорт"** в правом верхнем углу
3. Файл `equipment.xlsx` будет загружен

**Что экспортируется:**
- Всё оборудование с полными данными
- Все 7 столбцов в том же формате, что и для импорта
- Включая автосгенерированные инвентарные номера

---

## Обработка ошибок

### Принцип работы

При импорте система:
1. **Не останавливается** при ошибках в отдельных строках
2. **Пропускает** строки с ошибками
3. **Продолжает** обработку остальных строк
4. **Выводит детальный отчет** о всех ошибках

### Типы ошибок

#### 1. Ошибки валидации (до импорта)

**Отсутствие обязательных полей:**
```
Строка 5: Не указано краткое наименование организации (org_short_name_ru)
```

**Логические ошибки:**
```
Строка 18: Нельзя указать отдел без структурного подразделения
```

#### 2. Ошибки импорта (во время создания)

**Нарушение уникальности:**
```
Строка 25: Должность с таким названием уже существует в этом отделе
```

**Ошибки базы данных:**
```
Строка 30: Ошибка при сохранении: [детали ошибки]
```

### Пример отчета об ошибках

После импорта вы увидите:

```
✅ Успешно импортировано: 15 должностей
⚠️ Пропущено: 3 строки
❌ Ошибок: 3

Детали ошибок:
- Строка 5: Не указано обязательное поле position_name
- Строка 18: Нельзя указать отдел без структурного подразделения
```

### Рекомендации по исправлению ошибок

1. **Скопируйте сообщения об ошибках**
2. **Откройте исходный Excel-файл**
3. **Найдите указанные строки**
4. **Исправьте ошибки** согласно сообщениям
5. **Повторите импорт**

---

## Примеры файлов

### Минимальный пример импорта структуры

Файл с 2 столбцами (только обязательные поля):

| org_short_name_ru | position_name |
|-------------------|---------------|
| Тест | Директор |
| Тест | Бухгалтер |

### Полный пример импорта структуры

Файл со всеми 9 столбцами:

| org_short_name_ru | subdivision_name | department_name | position_name | safety_instructions_numbers | internship_period_days | is_responsible_for_safety | can_be_internship_leader | can_sign_orders |
|-------------------|------------------|-----------------|---------------|----------------------------|------------------------|---------------------------|--------------------------|-----------------|
| РиК | Цех №1 | Участок А | Слесарь | И-001, И-025 | 5 | нет | да | нет |
| РиК | Цех №1 | Участок А | Мастер | И-001 | 0 | да | да | да |

### Минимальный пример импорта сотрудников

Файл с 4 столбцами (только обязательные поля):

| hire_date | org_short_name_ru | position_name | full_name_nominative |
|-----------|-------------------|---------------|---------------------|
| 15.01.2020 | Тест | Директор | Иванов Иван Иванович |
| 20.02.2020 | Тест | Бухгалтер | Петрова Мария Сергеевна |

### Полный пример импорта сотрудников

Файл со всеми 6 столбцами:

| hire_date | org_short_name_ru | subdivision_name | department_name | position_name | full_name_nominative |
|-----------|-------------------|------------------|-----------------|---------------|---------------------|
| 15.01.2020 | РиК | Цех №1 | Участок А | Слесарь | Иванов Иван Иванович |
| 10.12.2019 | РиК | Цех №1 | Участок А | Мастер | Петров Петр Петрович |

### Минимальный пример импорта оборудования

Файл с 2 столбцами (только обязательные поля):

| org_short_name_ru | equipment_name |
|-------------------|----------------|
| Тест | Токарный станок |
| Тест | Фрезерный станок |

### Полный пример импорта оборудования

Файл со всеми 7 столбцами:

| org_short_name_ru | subdivision_name | department_name | equipment_name | inventory_number | maintenance_period_months | last_maintenance_date |
|-------------------|------------------|-----------------|----------------|------------------|---------------------------|-----------------------|
| РиК | Цех №1 | Участок А | Токарный станок 16К20 | 00000001 | 12 | 15.01.2025 |
| РиК | Цех №1 | Участок А | Фрезерный станок | | 6 | 01.02.2025 |

---

## FAQ

### Вопрос: Можно ли импортировать CSV файлы?
**Ответ:** Нет, система поддерживает только XLSX и XLS форматы.

### Вопрос: Что делать, если в файле 1000 строк и 50 из них с ошибками?
**Ответ:** Система импортирует 950 корректных строк, а 50 пропустит с выводом детального отчета об ошибках. Вы можете исправить эти 50 строк и импортировать их отдельно.

### Вопрос: Как обновить данные существующих должностей?
**Ответ:** Экспортируйте текущие данные, отредактируйте нужные поля в Excel, импортируйте обратно. Система найдет существующие должности по комбинации (название, организация, подразделение, отдел) и обновит их.

### Вопрос: Можно ли импортировать сотрудников без создания организации заранее?
**Ответ:** Да, система автоматически создаст минимальную организацию по указанному `org_short_name_ru`.

### Вопрос: Как работает автогенерация ФИО в дательном падеже?
**Ответ:** Система использует библиотеку pymorphy2 для морфологического анализа и склонения. В большинстве случаев работает корректно для русских ФИО.

### Вопрос: Можно ли импортировать должности без указания подразделения?
**Ответ:** Да, должность будет привязана только к организации. Просто оставьте столбцы `subdivision_name` и `department_name` пустыми.

### Вопрос: Что будет, если импортировать файл дважды?
**Ответ:**
- **Для структуры:** Существующие должности будут обновлены
- **Для сотрудников:** Существующие сотрудники (по ФИО) будут обновлены
- **Для оборудования:** Существующее оборудование (по inventory_number) будет обновлено

### Вопрос: Как работает автогенерация инвентарных номеров?
**Ответ:** Если поле `inventory_number` пустое, система автоматически генерирует 8-значный номер (00000001, 00000002 и т.д.), находя последний использованный номер и увеличивая его на 1.

### Вопрос: Можно ли указать свой инвентарный номер при импорте оборудования?
**Ответ:** Да, просто укажите нужный 8-значный номер в столбце `inventory_number`. Система использует его вместо автогенерации.

### Вопрос: Почему нет полей для группы электробезопасности при импорте?
**Ответ:** Эти поля намеренно исключены из импорта для упрощения процесса. Они заполняются вручную после импорта через админ-панель.

### Вопрос: Можно ли отменить импорт после подтверждения?
**Ответ:** Нет, после подтверждения импорта данные сохраняются в базу. Используйте функцию предпросмотра перед подтверждением.

### Вопрос: Какой максимальный размер файла для импорта?
**Ответ:** Ограничений нет, но рекомендуется импортировать не более 5000 строк за раз для оптимальной производительности.

### Вопрос: Почему дата начала работы (start_date) не указывается в файле?
**Ответ:** Для упрощения процесса импорта `start_date` автоматически копируется из `hire_date`. Если они различаются, отредактируйте после импорта.

### Вопрос: Все сотрудники импортируются со статусом "active" и типом договора "standard". Можно ли изменить?
**Ответ:** Эти значения установлены по умолчанию для упрощения массового импорта. Если нужны другие значения, отредактируйте после импорта через админ-панель.

### Вопрос: Где находятся кнопки импорта/экспорта?
**Ответ:** Кнопки "📥 Импорт" и "📤 Экспорт" находятся в правом верхнем углу страницы списка каждого справочника (рядом с кнопкой "Добавить").

---

## Техническая информация

### Используемые технологии
- **django-import-export** - библиотека для импорта/экспорта
- **openpyxl** - работа с XLSX файлами
- **xlrd** - работа с XLS файлами
- **pymorphy2** - морфологический анализ и склонение
- **tablib** - обработка табличных данных

### Структура файлов

```
directory/
├── resources/
│   ├── __init__.py
│   ├── organization_structure.py  # Resource для оргструктуры (9 полей)
│   ├── employee.py                # Resource для сотрудников (6 полей)
│   └── equipment.py               # Resource для оборудования (7 полей)
├── admin/
│   ├── position.py                # Admin для должностей с импортом/экспортом
│   ├── employee.py                # Admin для сотрудников с импортом/экспортом
│   └── equipment.py               # Admin для оборудования с импортом/экспортом
└── templates/
    └── admin/
        └── directory/
            ├── position/
            │   ├── import.html
            │   ├── import_preview.html
            │   └── change_list_tree.html (кнопки импорта/экспорта)
            ├── employee/
            │   ├── import.html
            │   ├── import_preview.html
            │   └── change_list_tree.html (кнопки импорта/экспорта)
            └── equipment/
                ├── import.html
                ├── import_preview.html
                └── change_list_tree.html (кнопки импорта/экспорта)
```

### Архитектура

Импорт/экспорт интегрирован непосредственно в админ-классы каждой модели:
- Каждый админ-класс содержит методы `import_view()` и `export_view()`
- URL-маршруты регистрируются в `get_urls()` каждого админа
- Кнопки добавлены в шаблоны `change_list_tree.html` каждой модели
- Предпросмотр импорта перед подтверждением
- Детальные отчеты об ошибках

### Особенности реализации импорта организационной структуры

**Ключевой момент:** Импорт организационной структуры использует метод `before_import_row` для создания связанных объектов (организации, подразделения, отделы) **перед** обработкой каждой строки.

**Последовательность выполнения:**

1. Django-import-export читает строку из Excel
2. Вызывается `before_import_row(row)`:
   - Извлекаются значения `org_short_name_ru`, `subdivision_name`, `department_name`
   - Создаются/находятся объекты Organization, StructuralSubdivision, Department
   - В `row` добавляются ID этих объектов: `row['organization'] = organization.id`
3. Django-import-export автоматически создает/обновляет Position, используя ID из row
4. В режиме dry_run (предпросмотр) все изменения откатываются транзакцией

**Почему это работает:**
- В `Meta.fields` указаны `organization`, `subdivision`, `department` - это позволяет django-import-export связать ID из row с ForeignKey полями модели
- Метод `get_instance()` находит существующую должность по комбинации (position_name, organization, subdivision, department)
- Булевы поля обрабатываются через кастомный `BooleanRussianWidget`, поддерживающий русские значения (да/нет)

---

## Контакты и поддержка

Если у вас возникли вопросы или проблемы с импортом/экспортом:

1. Проверьте данную документацию
2. Проверьте раздел FAQ
3. Обратитесь к системному администратору

---

**Последнее обновление:** 2025-10-01
**Версия системы:** 2.0
