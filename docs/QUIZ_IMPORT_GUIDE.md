# 📥 Руководство по импорту вопросов экзамена

## ✅ Реализовано

Создан полноценный веб-интерфейс для импорта вопросов с предпросмотром и валидацией.

---

## 🚀 Как использовать

### Шаг 1: Переход к импорту

**Вариант A - Из списка разделов:**
1. Админка → Directory → Quiz categories
2. Открыть нужный раздел
3. Внизу страницы кнопка: **"📥 Импортировать вопросы в этот раздел"**

**Вариант B - Прямая ссылка:**
- URL: `http://localhost:8001/directory/quiz/import/`

### Шаг 2: Загрузка файлов

Заполните форму:

1. **Раздел** - выберите из списка существующих
2. **Word файл (.docx)** - обязательно
3. **ZIP с изображениями** - опционально
4. **Заменить существующие** - чекбокс (если отмечен, удалит все вопросы в разделе)

Нажмите **"📤 Загрузить и просмотреть"**

### Шаг 3: Предпросмотр

Откроется страница с:
- **Статистикой**: сколько вопросов, сколько с правильными ответами
- **Валидацией**: список ошибок (если есть)
- **Примерами**: первые 5 вопросов для проверки
- **Действием**: добавить или заменить

### Шаг 4: Подтверждение

Нажмите **"✅ Подтвердить импорт X вопросов"**

Вопросы будут импортированы в базу данных.

---

## 📄 Формат Word документа

### Структура:

```
Вопрос № 1 из 148
Вопрос: Текст вопроса здесь?
Варианты ответа:
Первый вариант (обычный)
Второй вариант ЖИРНЫЙ - ПРАВИЛЬНЫЙ
Третий вариант (обычный)
Четвертый вариант (обычный)

Вопрос № 2 из 148
...
```

### Правила форматирования:

1. **"Вопрос № X из Y"** - жирный, начало нового вопроса
2. **"Вопрос:"** - жирный, после этого текст вопроса (обычный)
3. **"Варианты ответа:"** - жирный
4. **Правильный ответ** - выделен **ЖИРНЫМ** шрифтом (>70% текста параграфа)

### ⚠️ Важные ограничения:

- **Только ОДИН правильный ответ** на вопрос
- Минимум **2 варианта** ответа
- Нельзя несколько жирных ответов

---

## 🖼️ Импорт изображений

### Подготовка:

1. Создайте изображения с названиями по номеру вопроса:
   - `1.jpg` → вопрос №1
   - `2.png` → вопрос №2
   - `3.jpg` → вопрос №3
   - и т.д.

2. Запакуйте все в ZIP архив

3. Загрузите в форме импорта

### Поддерживаемые форматы:

- `.jpg` / `.jpeg`
- `.png`
- `.gif`

### Логика привязки:

```
Файл: 1.jpg    → Вопрос #1 .image = 1.jpg
Файл: 2.png    → Вопрос #2 .image = 2.png
Файл: 5.jpeg   → Вопрос #5 .image = 5.jpeg
```

Если для вопроса нет изображения - поле `image` останется пустым.

---

## 🛡️ Валидация

Перед импортом система проверяет:

### ✅ Обязательные проверки:

1. Каждый вопрос имеет текст
2. Минимум 2 варианта ответа
3. Есть хотя бы 1 правильный ответ (жирный)
4. **Только ОДИН** правильный ответ (не несколько)

### ⚠️ Если есть ошибки:

- Показываются в предпросмотре
- Можно импортировать с предупреждением
- Рекомендуется исправить в файле

---

## 📂 Технические детали

### Файлы:

```
directory/
├── forms/
│   └── quiz_import_form.py          # Формы загрузки
├── views/
│   └── quiz_import_views.py         # Логика импорта
├── urls.py                           # Маршруты
└── admin/
    └── quiz_admin.py                 # Кнопка в админке

templates/
├── directory/quiz/
│   ├── import_upload.html            # Шаг 1: загрузка
│   └── import_preview.html           # Шаг 2: предпросмотр
└── admin/directory/quizcategory/
    └── change_form.html              # Кнопка импорта
```

### Процесс импорта:

```
1. Загрузка файлов
   ↓
2. Сохранение во временную директорию
   ↓
3. Парсинг Word → список вопросов
   ↓
4. Распаковка ZIP с изображениями (если есть)
   ↓
5. Валидация вопросов
   ↓
6. Сохранение в сессию
   ↓
7. Предпросмотр (первые 5 вопросов)
   ↓
8. Подтверждение
   ↓
9. Импорт в БД:
   - CREATE Question
   - CREATE Answer (для каждого варианта)
   - Привязка изображений (если есть)
   ↓
10. Очистка временных файлов
```

### URL-маршруты:

- `/directory/quiz/import/` - загрузка
- `/directory/quiz/import/preview/` - предпросмотр
- `/directory/quiz/import/cancel/` - отмена

---

## 🧪 Тестирование

### Тестовый файл:

Используйте: `G:\Мой диск\OT_online\quiz\Общие вопросы по охране труда\Общие вопросы по охране труда_для импорта.docx`

**Ожидаемый результат:**
- 148 вопросов
- Все с правильными ответами

### Команда для проверки парсинга:

```bash
python manage.py import_quiz_questions_v2 "путь\к\файлу.docx" \
    --category="Тест" \
    --dry-run
```

Покажет распознанные вопросы без импорта в БД.

---

## 📝 Примеры использования

### Пример 1: Первый импорт

1. Админка → Quiz categories → Открыть "Общие вопросы по ОТ"
2. Кнопка "Импортировать вопросы"
3. Выбрать файл `Общие_вопросы.docx`
4. Оставить "Заменить существующие" **ВЫКЛЮЧЕННЫМ**
5. Загрузить → Проверить предпросмотр → Подтвердить

### Пример 2: Обновление вопросов

1. Импорт → Выбрать раздел
2. Выбрать новый файл
3. **ВКЛЮЧИТЬ** "Заменить существующие"
4. Загрузить → Подтвердить

Все старые вопросы удалятся, добавятся новые.

### Пример 3: С изображениями

Структура ZIP:
```
images.zip
├── 1.jpg
├── 2.jpg
├── 3.png
├── 5.jpg
└── 10.jpeg
```

Результат:
- Вопрос #1 → image = 1.jpg
- Вопрос #2 → image = 2.jpg
- Вопрос #3 → image = 3.png
- Вопрос #4 → image = NULL
- Вопрос #5 → image = 5.jpg
- ...
- Вопрос #10 → image = 10.jpeg

---

## 🐛 Возможные ошибки

### "Нет правильного ответа"

**Причина:** Ни один вариант не выделен жирным

**Решение:** Выделите правильный ответ жирным шрифтом в Word

### "Несколько правильных ответов"

**Причина:** Несколько вариантов жирные

**Решение:** Оставьте только ОДИН жирный ответ

### "Меньше 2 вариантов ответа"

**Причина:** Вопрос распознался без вариантов или с одним

**Решение:** Проверьте структуру в Word файле

### "Файл слишком большой"

**Причина:** Word > 10 МБ или ZIP > 50 МБ

**Решение:** Сжать изображения или разбить на несколько файлов

---

## ✨ Преимущества веб-импорта

✅ Визуальный предпросмотр перед импортом
✅ Валидация с понятными сообщениями
✅ Автоопределение правильных ответов по форматированию
✅ Автоматическая привязка изображений
✅ Защита от случайного удаления (подтверждение)
✅ Статистика импорта
✅ Работа через браузер (не нужна консоль)

---

## 🔜 Планы улучшений

- [ ] Экспорт вопросов обратно в Word/Excel
- [ ] Редактирование вопросов перед импортом
- [ ] Импорт из Excel
- [ ] Массовое редактирование правильных ответов
- [ ] История импортов
